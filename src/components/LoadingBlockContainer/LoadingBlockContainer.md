LoadingBlockContainer base example:

```js
import LoadingBlockContainer  from '$trood/components/LoadingBlockContainer';
const isLoading = true;

<LoadingBlockContainer {...{
  isBlocked: isLoading,
}}>
  some code
</LoadingBlockContainer>
```
