EnchancedSwitch base example:

```js
import EnchancedSwitch, { INNER_INPUT_TYPES } from '$trood/components/EnchancedSwitch';

<EnchancedSwitch {...{
    switched: true,
    type: INNER_INPUT_TYPES.radio,
    ...other,
}} />
```
